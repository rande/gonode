name= "GoNode - Codeship"
bind= ":2508"

[databases.master]
type    = "master"
dsn     = "postgres://{{ env "PG_USER" }}:{{ env "PG_PASSWORD" }}@localhost:5434/test"
enabled = true
prefix  = "test"


[filesystem]
path = "/tmp/gnode"

[auth]
key = "ZeSecretKey0oo"

    [auth.jwt]
        [auth.jwt.login]
        path = "/login"

        [auth.jwt.token]
        path = "^\\/nodes\\/(.*)$"
